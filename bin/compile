#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

BUILD_DIR=$1
CACHE_DIR=$2

APP_DIR="/app"

mkdir ~/.heroku/vendor
cd ~/.heroku/vendor
curl -O https://github.com/andyisnowskynet/cmake-buildpack-tar/blob/master/cmake-3.4.1-heroku.tar.gz?raw=true
tar -xvzf cmake-3.4.1-heroku.tar.gz